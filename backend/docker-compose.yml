services:
    mariadb:
        image: mysql:8.0.27
        ports:
            - 3306:3306
        env_file:
            - .env
        volumes:
            - ./db:/var/lib/mysql

    adminer:
        image: adminer:latest
        ports:
            - 9999:8080
